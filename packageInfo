# This packageInfo was automatically modified/generated by the
# `brazil' script.  Feel free to edit this file by hand as necessary,
# but please be aware that any usage of `brazil' will result in a loss
# of any comments, as well as whitespace formatting and key ordering.
# Set doNotModifyPackageInfo=true to keep 'brazil' from modifying
# this file.

base = {
	versionSet = live;
	workspace = WikiAutomation;
};
packages = {
	WikiBot-1.0 = .;
};
